export interface AnswerChipProps {
    color: string;
}

export interface SectionOverviewProps {
    section: number;
    attempts: number;
    score: number;
    time: number;
    results: number[];
}